========================================
  GitHub 同步指南
========================================

【你的 GitHub 仓库】
https://github.com/QJHWC/awsq.git


========================================
  方式一：一键同步（推荐）⭐
========================================

双击运行：同步到GitHub.bat

脚本会自动：
✓ 初始化 Git 仓库
✓ 添加远程仓库
✓ 添加所有文件
✓ 提交更改
✓ 推送到 GitHub


========================================
  方式二：手动命令行同步
========================================

【首次同步】

# 1. 初始化 Git 仓库
git init

# 2. 配置用户信息（首次使用 Git 需要）
git config --global user.name "QJHWC"
git config --global user.email "your-email@example.com"

# 3. 添加远程仓库
git remote add origin https://github.com/QJHWC/awsq.git

# 4. 添加所有文件
git add .

# 5. 提交
git commit -m "Initial commit: Amazon Q to OpenAI API Bridge"

# 6. 推送到 GitHub
git push -u origin main

# 如果 main 分支不存在，尝试：
git push -u origin master


【后续更新】

# 1. 添加修改的文件
git add .

# 2. 提交
git commit -m "Update: 更新说明"

# 3. 推送
git push


========================================
  GitHub 认证配置
========================================

【方式一】使用 Personal Access Token（推荐）

1. 访问 GitHub 生成 Token：
   https://github.com/settings/tokens

2. 点击 "Generate new token (classic)"

3. 设置权限：
   ☑ repo (完整权限)
   ☑ workflow
   
4. 生成并复制 Token（格式：ghp_xxxxx）

5. 推送时使用：
   用户名：QJHWC
   密码：粘贴你的 Token


【方式二】使用 Git Credential Manager

安装 Git for Windows 时会自动安装，推送时会弹出登录窗口


【方式三】使用 SSH Key

1. 生成 SSH Key:
   ssh-keygen -t ed25519 -C "your-email@example.com"

2. 添加到 GitHub:
   https://github.com/settings/keys
   
3. 修改远程仓库地址:
   git remote set-url origin git@github.com:QJHWC/awsq.git


========================================
  .gitignore 说明
========================================

已自动排除以下文件/目录：
✓ .env（敏感信息）
✓ data.sqlite3（数据库）
✓ __pycache__/（Python缓存）
✓ .venv/（虚拟环境）
✓ logs/（日志文件）
✓ screenshots/（截图文件）

这些文件不会被提交到 GitHub


========================================
  常见问题
========================================

Q: 推送时提示 "fatal: 'origin' does not appear to be a git repository"
A: 运行：git remote add origin https://github.com/QJHWC/awsq.git

Q: 推送时提示认证失败
A: 需要使用 Personal Access Token 而不是密码

Q: 推送时提示 "Updates were rejected"
A: 先拉取：git pull origin main --allow-unrelated-histories
   然后推送：git push origin main

Q: 如何查看当前状态？
A: 运行：git status

Q: 如何查看提交历史？
A: 运行：git log

Q: 如何撤销未提交的修改？
A: 运行：git checkout .

Q: 如何删除远程仓库中的文件？
A: git rm --cached 文件名
   git commit -m "Remove file"
   git push


========================================
  完整同步流程
========================================

【第一次同步】

1. 确保已安装 Git
   下载：https://git-scm.com/download/win

2. 双击运行：同步到GitHub.bat

3. 输入提交说明（或直接回车）

4. 如果提示认证，输入：
   - 用户名：QJHWC
   - 密码：你的 Personal Access Token

5. 等待推送完成

6. 访问查看：https://github.com/QJHWC/awsq


【后续更新】

1. 修改代码后

2. 双击运行：同步到GitHub.bat

3. 输入本次更新说明

4. 自动推送到 GitHub


========================================
  推送后的效果
========================================

GitHub 仓库将包含：

📁 amazonq2api/
├── 📄 README.md（项目文档）
├── 📄 app.py（主程序）
├── 📄 amazonq_auto_register.py（自动注册）
├── 📄 requirements.txt（依赖列表）
├── 📄 Dockerfile（Docker配置）
├── 📄 docker-compose.yml（Docker Compose）
├── 📂 frontend/（Web控制台）
├── 🚀 启动公网服务.bat
├── 🚀 一键部署_Cloudflare.bat
├── 🤖 开始注册.bat
├── 🤖 批量注册.bat
├── 📖 开始部署.txt
├── 📖 快速部署指南.txt
├── 📖 部署说明.txt
└── ... 其他文件

❌ 不会包含：
- .env（API密钥）
- data.sqlite3（数据库）
- logs/（日志）
- .venv/（虚拟环境）


========================================
  分享你的项目
========================================

项目推送成功后，其他人可以：

1. 克隆仓库：
   git clone https://github.com/QJHWC/awsq.git

2. 安装依赖：
   pip install -r requirements.txt

3. 启动服务：
   python -m uvicorn app:app --host 0.0.0.0 --port 8000

4. 自动注册账号：
   双击：开始注册.bat


========================================
  保护敏感信息
========================================

⚠️ 重要提示：

1. .env 文件已被排除
   你的 API 密钥 (sk-790214) 不会被上传

2. data.sqlite3 已被排除
   你的账号数据不会被上传

3. 克隆项目后需要：
   - 自行创建 .env 文件
   - 自行注册 Amazon Q 账号

4. 如果不小心提交了敏感信息：
   # 从历史中删除
   git filter-branch --force --index-filter \
   "git rm --cached --ignore-unmatch .env" \
   --prune-empty --tag-name-filter cat -- --all
   
   # 强制推送
   git push origin --force --all


========================================
  Git 常用命令速查
========================================

# 查看状态
git status

# 查看差异
git diff

# 添加文件
git add .
git add 文件名

# 提交
git commit -m "说明"

# 推送
git push

# 拉取
git pull

# 查看日志
git log
git log --oneline

# 查看远程仓库
git remote -v

# 创建分支
git branch 分支名
git checkout -b 分支名

# 切换分支
git checkout 分支名

# 合并分支
git merge 分支名

# 撤销修改
git checkout -- 文件名
git reset --hard HEAD


========================================
  获取帮助
========================================

Git 官方文档：https://git-scm.com/doc
GitHub 文档：https://docs.github.com
Git 教程：https://www.runoob.com/git/git-tutorial.html

